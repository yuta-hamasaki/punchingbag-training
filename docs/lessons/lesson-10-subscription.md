# Lesson 10: Stripeサブスク

## このレッスンの目的

完成済みの画面・型・ボイラープレートを使い、Premium判定、署名検証、Webhook同期の重要部分だけを穴埋めします。

## 完成後にできること

- Premium判定、署名検証、Webhook同期を自分の言葉で説明できる
- 正常系だけでなく境界値・失敗時の挙動を確認できる
- UI、サーバー、外部サービスの責務を混ぜずに実装できる

## 前提知識

Lesson 09までのTODOがレビュー済みであること。未完了のまま先へ進みません。

## 今回触るファイル

- `src/lib/subscription/access.ts / src/lib/stripe/webhook.ts`
- `tests/lessons/lesson-10.test.*`

## 重要な概念

Premium判定、署名検証、Webhook同期を扱います。骨格は完成しているため設定の転記ではなく、実際の判断・状態遷移・安全性に集中します。実務では入力を信用せず、後始末を確実にし、外部境界の失敗を利用者向けの状態へ変換します。UIでは運動中の視認性と操作性、サーバーでは認証・所有権・入力検証、3Dでは端末負荷を優先します。

## 実装手順

1. 対象ファイルの型と完成済みの周辺コードを読む。
2. `L10-T01`, `L10-T02`を上から1問ずつ実装する。
3. 各問のテストを実行し、失敗メッセージから条件を確認する。
4. 困った場合だけHint 1から順に確認する。
5. 実装を提出してレビューを受ける。

## TODO一覧

- `L10-T01`: コードコメントに書かれた3条件を満たす重要ロジック
- `L10-T02`: コードコメントに書かれた3条件を満たす重要ロジック

## 動作確認方法

```bash
npm run test:lesson-10
npm run typecheck
npm run lint
```

## よくあるエラー

- 仮のreturn値や`YOUR CODE HERE`を残す
- 型エラーを`any`や型アサーションで隠す
- 正常系だけ実装してnull、境界値、拒否、cleanupを扱わない
- UIだけで認可し、サーバー側の確認を忘れる

## 理解確認問題

1. このロジックをReactやRoute Handlerから分離する利点は何ですか。
2. 失敗時に資源や状態が残ると何が起きますか。
3. テストすべき境界値を2つ挙げてください。

## 次のレッスン

TODOのレビューとテスト完了後にLesson 11へ進みます。
